import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: 53,
    height: 53,
    borderRadius: 8,
    alignItems: "center",
    justifyContent: 'center',
    marginRight: 22,
    
  },
  avatar: {
    width: '92%',
    height: '92%',
    borderRadius: 8,
  },
});